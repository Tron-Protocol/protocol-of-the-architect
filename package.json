{
  "name": "protocol-of-the-architect",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "validate": "node scripts/validate-source.mjs",
    "generate:support": "node scripts/generate-support-files.mjs",
    "build": "npm run validate && npm run generate:support && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "latest",
    "astro": "latest"
  },
  "devDependencies": {}
}
