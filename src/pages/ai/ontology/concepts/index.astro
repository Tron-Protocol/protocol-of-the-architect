---
import AiPageLayout from '@/layouts/AiPageLayout.astro';
import OntologyRecordGroup from '@/components/OntologyRecordGroup.astro';
import { getOntologyModule } from '@/lib/content.js';
const module = getOntologyModule('concepts');
---
<AiPageLayout title="Ontology: Concepts" description={module.public_role}>
  <OntologyRecordGroup title="Concepts" description="Public display is grouped on this module page. Internal trace remains hidden." records={module.records} />
</AiPageLayout>
