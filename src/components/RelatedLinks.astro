---
import { withBase } from '@/lib/routes.js';
const { links = [] } = Astro.props;
---
<nav class="route-nav" aria-label="Related links">
  {links.map(link => <a href={withBase(link.href)}>{link.label}</a>)}
</nav>
